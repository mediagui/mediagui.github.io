# this file uses prebuilt image in dockerhub
services:
  jekyll:
    image: amirpourmand/al-folio:slim
    #build: .
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - .:/srv/jekyll:z
    environment:
      - JEKYLL_ENV=development
      - LISTEN_GEM_FORCE_POLLING=true
